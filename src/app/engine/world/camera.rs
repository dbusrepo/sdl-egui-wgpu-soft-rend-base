pub(super) struct Camera {
    // position: glam::Vec3,
    // rotation: glam::Quat,
}

impl Camera {
    pub(super) fn new() -> Self {
        Self {}
    }

    pub(super) fn update(&mut self, _step_time: f64) {}
}
